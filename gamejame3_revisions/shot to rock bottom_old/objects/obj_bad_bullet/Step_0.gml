script_execute(states_array[state]);
if(distance_to_point(room_width/2,room_height/2) > room_width){
	instance_destroy(self);
}
image_xscale = power(2.7,-b*t)*cos(t - a) + 1;
image_yscale = power(2.7,-b*t)*cos(t + a) + 1;
t += 1;
